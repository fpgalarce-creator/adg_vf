{
  "name": "adg-vf",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix client run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
